# MongoDB Atlas connection string
# Create a free cluster at https://cloud.mongodb.com and get the URI
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Optional: database name (defaults to qr-release)
# MONGODB_DB_NAME=qr-release
